# Klubb1 Tivoli: one language-model call per request, so limit per client IP.
# A club evening is 30 people behind ONE Wi-Fi address, so this must be generous;
# the daily caps in /etc/klubb1.env are what actually bound the bill.
limit_req_zone $binary_remote_addr zone=klubb1_api:10m rate=120r/m;
