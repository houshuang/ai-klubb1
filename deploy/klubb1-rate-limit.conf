# Klubb1 Tivoli: one language-model call per request, so limit per client IP.
# 20 requests/minute with a burst of 10 is plenty for a room of kids on one
# Wi-Fi (one shared IP) and useless for bulk abuse.
limit_req_zone $binary_remote_addr zone=klubb1_api:10m rate=20r/m;
